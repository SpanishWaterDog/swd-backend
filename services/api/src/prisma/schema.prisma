generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Exercise {
  id          String @id @default(uuid())
  name        String
  description String
  s3Path      String
}